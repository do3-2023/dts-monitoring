kind: PersistentVolume
apiVersion: v1
metadata:
  name: pv-database
  namespace: data
  labels:
    type: local
spec:
  storageClassName: manual
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "./db_data"