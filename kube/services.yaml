apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: data
spec:
  ports:
  - name: db
    port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    name: db
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: web-${TARGET_ROLE}
  namespace: front
spec:
  ports:
    - name: web
      port: 80
      protocol: TCP
      targetPort: 4000 
  selector:
    name: web-${TARGET_ROLE}
  type: LoadBalancer
  loadBalancerIP: 162.38.112.92
---
apiVersion: v1
kind: Service
metadata:
  name: api-${TARGET_ROLE}
  namespace: back
spec:
  ports:
  - name: api
    port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    name: api-${TARGET_ROLE}
  type: LoadBalancer
  loadBalancerIP: 162.38.112.79